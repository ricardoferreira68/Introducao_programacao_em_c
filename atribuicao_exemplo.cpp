#include <iostream>
using namespace std;
int main(void)
{
    int A, C, i;

    A  = 50;   //Atribui o valor 50 à variável A.
    cout << "A: " << A << endl;

    C  = A + 100;   //Atribui o valor 150 à variável C.
    cout << "C: " << C << endl;

    i  = 1;   //Atribui o valor 1 à variável i.
    cout << "i: " << i << endl;

    i  = i + 1;   //Atribui o valor 2 à variável i.
    cout << "i: " << i << endl;
    return(0);
}
